Bootstrap: docker
From: python:3.8
%setup


%files
    requirements.txt requirements.txt

%post
    apt-get -y update
    ls
    pip install -r requirements.txt


%runscript
    pwd
    ls
    python3 main.py
